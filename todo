* Move packages into a packages file in 'profiles'
 * have different 'package sets' globally, allow configs to pull from them.
 * keep comma separated, space separate programatically where needed
  * create library function for this
* Get user config (username/password/sshkey) from an rc file
* Better docs
 * Better describe how a config works
* figure out if travis will work (sudo?)
